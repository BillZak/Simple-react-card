import React from 'react';

const Description = (props) => {
    return (
        <div className="description">
            <p>
                {props.description}
            </p>
        </div>
    );
}

export default Description;
